# `example2-serde-optional`

Typical usage in a library where `serde` is a mandatory dependency

---

The library in this example has its [documentation](https://docs.rs/lib-conf-example-2/latest) published to showcase
the structs/docs generated by `lib-conf`.

> [!NOTE]
> The remaining examples will not contain the extra 'noise' fields.
>
> These were provided in the first examples to show the context of
> how `lib-conf` fits into an everyday rust app.
