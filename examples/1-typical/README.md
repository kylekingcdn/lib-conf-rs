# `example1-typical`

Typical usage in a library where `serde` is a mandatory dependency

---

The library in this example has its [documentation](https://docs.rs/lib-conf-example-1/latest) published to showcase
the structs/docs generated by `lib-conf`.
