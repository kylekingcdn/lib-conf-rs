[package]
name = "lib-conf-example-readme"
description = "This is an example for `lib_conf` and does not provide actual functionality."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
publish = true

[lib]
name = "lib_conf_example_readme"

[[bin]]
name = "example-readme"
path = "src/bin.rs"

[dependencies]
config = { workspace = true }
dotenvy = { workspace = true }
lib-conf = { workspace = true, features = ["serde"] }
serde = { workspace = true, features = ["derive"] }

[lints]
workspace = true
