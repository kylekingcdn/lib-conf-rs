# we can still override the required field, but we can't unset it (no default to fallback to)
APP__EX3__ENABLE_TELEMETRY=false

# this has no effect since `override_skip` was used on this field in the lib
APP__EX3__CLIENT_NAME="cant change this"

APP__EX3__LOG_FILE_PATH=/var/log/app.log