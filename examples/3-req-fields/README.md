# `example3-req-fields`

This example showcases a library with settings that must be initialized

---

The library in this example has its [documentation](https://docs.rs/lib-conf-example-3/latest) published to showcase
the structs/docs generated by `lib-conf`.
